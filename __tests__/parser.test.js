import Parser from '../src/types/parser'
import {SAMPLE, NUMBERS} from './utils/sampleIngredients'

describe('parses ingredients successfully', () => {

    test('adds numeric amounts to amount object', () => {
        
    })
})